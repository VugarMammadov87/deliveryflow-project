"""Executable ETL Spark applications.

Modules in this package are entrypoints for local platform jobs. They are kept
small and explicit so Makefile, Airflow, and smoke-test commands can call the
same code path.
"""
