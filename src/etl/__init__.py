"""ETL application package for DeliveryFlow.

This namespace contains executable data-processing apps, not shared web
application code. It keeps Spark jobs importable for tests while still allowing
the same files to be submitted directly with `spark-submit`.
"""
